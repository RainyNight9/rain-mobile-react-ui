import { css } from 'docz-plugin-css'

export default {
  title: 'Rain Mobile React UI',
  description: 'A Mobile Design UI library for React',
  plugins: [
    css({
      preprocessor: 'postcss'
    })
  ]
}
